name: Daily Shorts Candidate Analysis

on:
  schedule:
    # 07:00 KST == 22:00 UTC (previous day)
    - cron: "0 22 * * *"
  workflow_dispatch:
    inputs:
      program:
        description: "Limit to one program (leedaeho|kyungjeshow). Leave empty to run both."
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: radihola-data-${{ github.ref }}
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        program: [leedaeho, kyungjeshow]
    steps:
      - uses: actions/checkout@v4
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}

      - uses: actions/setup-python@v5
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        with:
          python-version: "3.11"

      - name: Install system dependencies
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends ffmpeg fonts-nanum

      - name: Install python dependencies
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        run: pip install -r requirements.txt

      # YouTube often blocks cloud/CI IPs with "Sign in to confirm you're not
      # a bot". config.py's player-client fallback avoids this for most
      # requests; if YOUTUBE_COOKIES is set (exported from a real logged-in
      # browser session), use it too as a stronger fallback.
      - name: Write YouTube cookies (optional)
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        env:
          YOUTUBE_COOKIES: ${{ secrets.YOUTUBE_COOKIES }}
        run: |
          if [ -n "$YOUTUBE_COOKIES" ]; then
            printf '%s\n' "$YOUTUBE_COOKIES" > "$RUNNER_TEMP/youtube-cookies.txt"
            echo "YTDLP_COOKIES_FILE=$RUNNER_TEMP/youtube-cookies.txt" >> "$GITHUB_ENV"
          fi

      - name: Analyze
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PROGRAM: ${{ matrix.program }}
          PYTHONPATH: src
        run: python -m radihola.cli analyze --program "$PROGRAM"

      - name: Commit and push results
        if: ${{ github.event.inputs.program == '' || github.event.inputs.program == matrix.program }}
        env:
          PROGRAM: ${{ matrix.program }}
          BRANCH: ${{ github.ref_name }}
        run: |
          git config user.name "radihola-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No candidate changes to commit."
            exit 0
          fi
          git commit -m "data: shorts candidates for $PROGRAM ($(date -u +%F))"
          for attempt in 1 2 3 4 5; do
            if git push origin "HEAD:$BRANCH"; then
              exit 0
            fi
            echo "push failed, retrying after rebase (attempt $attempt)"
            git fetch origin "$BRANCH"
            git rebase "origin/$BRANCH"
            sleep $((attempt * 5))
          done
          echo "failed to push after retries" >&2
          exit 1
